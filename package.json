{
  "name": "bizjaiak",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client-install": "cd client && npm install",
    "client-build": "cd client && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "nanoid": "^4.0.2"
  }
}
